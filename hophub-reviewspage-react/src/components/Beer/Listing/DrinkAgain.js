import styled from "styled-components";

const DrinkAgain = styled.div`
  font-size: 14px;
  padding: 0 0 1% 0;
  text-transform: capitalize;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
`;

export default DrinkAgain;
